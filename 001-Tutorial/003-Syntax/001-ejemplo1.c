#include <stdio.h> // incluye funciones de entrada y salida, en este caso printf()

int main() // funcion de ejecucion del programa
{
    printf("Hello World!"); // imprime por pantalla lo que le pasamos como parametro
    return 0; // fin del programa
}
