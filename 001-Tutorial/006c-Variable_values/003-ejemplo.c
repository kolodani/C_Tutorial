#include <stdio.h>

int main()
{
    // Create a variable and assing the value 15 to it
    int myNum = 15;

    // Declare a variable without assinging it a value
    int myOtherNum;

    // Assing the value of myNum to myOtherNum
    myOtherNum = myNum;

    // myOtherNum now has 15 as a value
    printf("%d", myOtherNum);

    return 0;
}
